# Prompt

A file "test-recipe.jpg" got added in backend > test/resources/test-recipe.jpg.
Create an integration test that successfully imports the data in the database through the import API.

Prerequisite for the test: You can assume that the podman-compose is running, so that you have postgres up and running.

Don't stop before the test works (and add some data validation on the content too)